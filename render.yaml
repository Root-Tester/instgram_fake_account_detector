services:
  - type: web
    name: instagram-fake-account-detector
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: bash run_app.sh
    healthCheckPath: /_stcore/health
    envVars:
      - key: PYTHON_VERSION
        value: "3.13.0"
