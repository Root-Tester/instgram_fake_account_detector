services:
  - type: web
    name: instagram-fake-account-detector
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: ./run_app.sh
    healthCheckPath: /_stcore/health