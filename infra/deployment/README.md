# Deployment assets

Deployment-specific documentation and helper configuration belong here.
`render.yaml` remains at the repository root because Render Blueprint discovery
expects that conventional path.
